root:
  - path: c:\etc\mesosphere\setup-flags\repository-url
    permissions: "0644"
    owner: root
    content: |
      {{ bootstrap_url }}
  - path: c:\etc\mesosphere\setup-flags\cluster-package-list
    permissions: "0644"
    owner: root
    content: |
      {{ cluster_package_list_id }}
  - path: c:\etc\mesosphere\docker_credentials
    permissions: "0600"
    content: |
        {{ cluster_docker_credentials }}
